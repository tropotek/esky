name: esky

services:
  esky:
    build:
      context: .
      target: prod
    container_name: esky
    ports:
      # Bind to the LAN interface explicitly. Per-profile bearer tokens landed
      # in Phase 1.5, so LAN exposure is supported — but issue tokens before
      # widening this, and do not reach for 0.0.0.0 when a LAN IP will do.
      - "${ESKY_BIND:-127.0.0.1}:${ESKY_PORT:-8080}:8080"
    volumes:
      - ./data:/data
      - esky-models:/models
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  # Development harness. Not started by `docker compose up`.
  # Run with: docker compose run --rm test pytest -v
  test:
    build:
      context: .
      target: dev
    profiles: ["dev"]
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - esky-models:/models
    environment:
      PYTHONHASHSEED: "0"
      # The container runs as root against bind-mounted source; without this
      # it litters root-owned __pycache__ dirs the host user cannot delete.
      PYTHONDONTWRITEBYTECODE: "1"
      ESKY_DATA_DIR: /tmp/esky-data

volumes:
  esky-models:
